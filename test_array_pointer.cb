// test_array_pointer.cb
// new[]で作成した配列へのポインタアクセステスト

int main() {
    println("=== Testing array pointer access ===");
    
    int* arr = new int[5];
    println("Allocated int[5] at {hex(arr)}");
    
    // Can we access elements?
    // This might not work yet in Cb
    println("Test complete");
    
    delete arr;
    return 0;
}
