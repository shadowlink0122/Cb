long ans[1000];

long fib(int n){
    if(n < 0) return 0;
    if(ans[n] > 0) return ans[n];
    if(n == 0){
        ans[n] = 0;
    }else if(n == 1){
        ans[n] = 1;
    }else{
        // メモ化（希望があれば判定追加可能）
        ans[n] = fib(n - 1) + fib(n - 2);
    }
    return ans[n];
}

int main(){
    const int FIB_NUM = 92; // 答えがlongの最大値になる
    fib(FIB_NUM);
    for(int i = 0;i <= FIB_NUM;i++){
        print(ans[i]);
    }
    return 0;
}