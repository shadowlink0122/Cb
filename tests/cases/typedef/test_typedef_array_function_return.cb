// typedef配列定義と配列戻り値の複合テストケース
const int TEN = 10;
typedef int[TEN] Array;

Array ten(int n) {
    Array result;
    for (int i = 0; i < TEN; i++) {
        result[i] = n;
    }
    return result;
}

int main() {
    Array result = ten(8);
    
    // 戻り値の値検証
    for (int i = 0; i < TEN; i++) {
        if (result[i] != 8) {
            println("ERROR: result[%d] should be 8, got %d", i, result[i]);
            return 1;
        }
    }
    
    for (int i = 0; i < TEN; i++) {
        println("%d %d", i, result[i]);
    }
    
    println("Typedef array function return test passed");
    return 0;
}
