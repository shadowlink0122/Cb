# Cb Standard Library Tests (Cb Language)

**ディレクトリ**: `tests/cases/stdlib/`  
**目的**: stdlibをimportして実際に使用するテスト（ユーザー視点）

---

## 📖 概要

このディレクトリには、Cb標準ライブラリのCb言語レベルのテストが含まれています。

import/export機能を使用して、実際のライブラリ利用者と同じ方法でstdlibをテストします。

---

## 📂 カテゴリ構造

```
tests/cases/stdlib/
├── README.md                  # このファイル
├── allocators/                # アロケータ関連
│   ├── README.md
│   ├── test_system_allocator.cb
│   └── test_bump_allocator.cb
├── collections/               # コレクション（データ構造）
│   ├── README.md
│   ├── test_vector.cb
│   ├── test_queue.cb         # 今後追加
│   ├── test_stack.cb         # 今後追加
│   └── test_map.cb           # 今後追加
├── async/                     # 非同期処理（今後追加予定）
│   ├── test_task_queue.cb
│   └── test_event_loop.cb
└── io/                        # 入出力（今後追加予定）
    ├── test_file.cb
    └── test_stream.cb
```

---

## 🎯 カテゴリ説明

### allocators/ - アロケータ

メモリ管理に関するライブラリ

- **SystemAllocator**: malloc/freeラッパー（OS環境）
- **BumpAllocator**: リニアアロケータ（ベアメタル環境）

### collections/ - コレクション

データ構造に関するライブラリ

- **Vector**: 動的配列
- **Queue**: FIFOキュー（今後）
- **Stack**: LIFOスタック（今後）
- **Map**: 連想配列（今後）

### async/ - 非同期処理

非同期実行・並行処理に関するライブラリ

- **Task**: タスク基本単位
- **TaskQueue**: 優先度付きタスクキュー（Task[100]配列形式）
- **EventLoop**: イベントループ（今後）

### io/ - 入出力（今後）

ファイル・ストリーム入出力に関するライブラリ

- **File**: ファイル操作
- **Stream**: ストリーム処理

---

## 🧪 テストの実行方法

### 全てのstdlibテストを実行

```bash
make stdlib-test-cb
```

### カテゴリ別実行

```bash
# アロケータテスト
./main tests/cases/stdlib/allocators/test_system_allocator.cb
./main tests/cases/stdlib/allocators/test_bump_allocator.cb

# コレクションテスト
./main tests/cases/stdlib/collections/test_vector.cb
```

---

## 📝 新しいカテゴリの追加

### 1. カテゴリフォルダ作成

```bash
mkdir -p tests/cases/stdlib/new_category
```

### 2. READMEを作成

```bash
touch tests/cases/stdlib/new_category/README.md
```

### 3. テストファイル作成

```cb
// tests/cases/stdlib/new_category/test_module.cb
import "stdlib/new_category/module.cb";

void test_module_basic() {
    println("✅ Test passed");
}

void main() {
    test_module_basic();
}
```

### 4. Makefileに追加

```makefile
stdlib-test-cb: $(MAIN_TARGET)
	@echo "\n[NewCategory]"
	@echo "[1/1] Testing Module..."
	@./$(MAIN_TARGET) tests/cases/stdlib/new_category/test_module.cb
```

---

## 📊 テストカバレッジ

| カテゴリ | テストファイル数 | テスト数 | カバレッジ |
|---------|---------------|---------|----------|
| **allocators** | 2 | 4 | 80% |
| **collections** | 1 | 3 | 60% |
| **async** | 0 | 0 | 0% |
| **io** | 0 | 0 | 0% |
| **合計** | **3** | **7** | **70%** |

---

## ✅ テスト追加チェックリスト

新しいstdlibモジュールをテストする際:

- [ ] モジュールのカテゴリを決定
- [ ] 適切なカテゴリフォルダ配下にテストファイル作成
- [ ] importでstdlibファイルを読み込み
- [ ] 基本的な使用例をテスト
- [ ] assertで検証
- [ ] Makefileに追加
- [ ] `make stdlib-test-cb` で実行可能
- [ ] カテゴリREADMEを更新

---

## 🔗 関連ディレクトリ

- **stdlib実装**: `stdlib/`
- **C++統合テスト**: `tests/stdlib/`
- **テストドキュメント**: `docs/testing/stdlib_test_structure.md`

---

**最終更新**: 2025年10月28日  
**メンテナ**: GitHub Copilot
