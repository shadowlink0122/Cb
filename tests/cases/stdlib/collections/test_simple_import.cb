// Simple import test - Check if Queue<T> can be imported and used
import "stdlib/collections/queue.cb";

int main() {
    println("Queue<T> imported successfully!");
    
    // Test Queue<int>
    Queue<int> q;
    println("Queue<int> created!");
    
    q.enqueue(10);
    println("Queue<int> enqueue works!");
    
    int val = q.dequeue();
    println("Queue<int> dequeue works! Got: ", val);
    
    // Explicitly destroy before return
    q.destroy();
    println("Queue<int> destroyed!");
    
    return 0;
}
