struct Point {
    int x;
    int y;
};

void test_simple() {
    Point[3] points;
    points[0].x = 10;
    points[0].y = 20;
    
    Point* ptr = &points[0];
    
    // まずポインタ配列アクセスだけをテスト
    int x_val = ptr[0].x;
    
    println("x_val = %d", x_val);
}

void main() {
    test_simple();
}
