// シンプルなポインタ配列テスト

void main() {
    println("=== Test: Simple pointer array ===");
    int a = 10, b = 20, c = 30;
    int*[3] ptrs = [&a, &b, &c];
    
    println(*ptrs[0]);  // 10
    println(*ptrs[1]);  // 20
    println(*ptrs[2]);  // 30
    
    println("=== Test passed ===");
}
