// ポインタ同士の加算エラーテスト

int main() {
    int[10] arr = {0, 1, 2, 3, 4};
    int* p = arr;
    int* q = arr;
    
    // これはエラーになるべき
    int* invalid = p + q;
    
    println("This should not compile");
    return 0;
}
