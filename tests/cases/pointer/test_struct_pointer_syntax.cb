// 構造体ポインタの基本テスト

struct Point {
    int x;
    int y;
};

int main() {
    println("Test: Struct pointer");
    
    Point p = {x: 10, y: 20};
    
    // 構造体のポインタ変数を宣言（シンプルな形式）
    int* test_ptr = nullptr;
    println("Simple pointer works");
    
    // 構造体ポインタを試す
    // Point* pp = &p;  // これがエラーになるか確認
    
    return 0;
}
