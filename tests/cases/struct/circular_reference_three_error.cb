// 3つの構造体の循環参照エラー: A -> B -> C -> A

struct A {
    int a_value;
    B b_member;  // ❌ 値メンバー
};

struct B {
    int b_value;
    C c_member;  // ❌ 値メンバー
};

struct C {
    int c_value;
    A a_member;  // ❌ 値メンバー → A -> B -> C -> A の循環
};

int main() {
    A a;
    a.a_value = 1;
    return 0;
}
