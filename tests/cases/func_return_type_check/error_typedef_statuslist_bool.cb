// 戻り値型エラー：typedef StatusList（bool[]）にboolを返すエラー

typedef bool[] StatusList;

StatusList get_status_list() {
    return true; // エラー：StatusList (bool[])を期待しているのにboolを返している
}

int main() {
    StatusList statuses = get_status_list();
    println("First status: ", statuses[0]);
    return 0;
}
