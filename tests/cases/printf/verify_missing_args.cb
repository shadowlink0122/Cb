// 引数不足のテスト - 期待する動作を確認
int main() {
    // 引数が不足している場合、フォーマット指定子がそのまま表示されることを期待
    println("%d %d", 10);     // "10 %d" と表示されるはず
    println("%s %d");         // "%s %d" と表示されるはず
    println("値: %d, 名前: %s", 42); // "値: 42, 名前: %s" と表示されるはず
    
    return 0;
}
