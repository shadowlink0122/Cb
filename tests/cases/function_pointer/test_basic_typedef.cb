// 基本的な関数ポインタtypedef宣言のテスト

int add(int a, int b) {
    return a + b;
}

int main() {
    // 関数ポインタの直接宣言（typedefなし）
    int* op = &add;
    int result = op(5, 3);
    println(result); // 期待値: 8

    // 直接呼び出しもテスト
    int result2 = op(10, 20);
    println(result2); // 期待値: 30

    return 0;
}
