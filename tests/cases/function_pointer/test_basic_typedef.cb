// 基本的な関数ポインタtypedef宣言のテスト

int add(int a, int b) {
    return a + b;
}

int main() {
    // 将来的に関数ポインタ変数の宣言・代入・呼び出しを実装する
    int* op = &add;
    int result = (*op)(5, 3);
    println(result); // 期待値: 8
    
    // 現在はtypedef宣言がパースできることを確認
    // println("Function pointer typedef test");
    // println("BinaryOp type declared successfully");
    
    return 0;
}
