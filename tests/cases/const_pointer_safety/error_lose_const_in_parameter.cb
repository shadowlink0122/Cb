// const * const型情報が関数パラメータで失われることを確認するテスト
// 現在はconst * constをパラメータに渡すとエラーになるはず

void modify_through_param(int* ptr) {
    *ptr = 100;  // 非constポインタなので変更可能
}

void main() {
    int x = 42;
    const int* const ptr = &x;  // const * const型
    
    // ❌ 期待: const * constを非constポインタパラメータに渡すとエラーになるべき
    // しかし現在の実装では型情報が失われ、エラーが出ない可能性がある
    modify_through_param(ptr);  // この呼び出しは型安全性違反のはず
    
    println(x);  // xが100に変更されてしまう可能性
}
