// const T* const のポインタ再代入エラーテスト
// ポインタ自体の再代入が禁止されることを確認

int main() {
    int x = 10, y = 20;
    const int* const ptr = &x;
    
    println("Attempting to reassign const * const pointer...");
    ptr = &y;  // Error: Cannot reassign const pointer
    
    return 0;
}
