// typedef配列を返す関数のテストケース
const int TEN = 10;
typedef int[TEN] Array;

Array ten(int n) {
    Array result;
    for (int i = 0; i < TEN; i++) {
        result[i] = n;
    }
    return result;
}

int main() {
    Array result = ten(8);
    
    // 戻り値の配列内容を確認
    for (int i = 0; i < TEN; i++) {
        println(i, result[i]);
    }
    
    return 0;
}
