int main() {
    // 10次元配列テスト（各次元のサイズを小さく）
    int[2][1][1][1][1][1][1][1][1][2] arr10d = [
        [[[[[[[[[1, 2]]]]]]]]], 
        [[[[[[[[[3, 4]]]]]]]]]
    ];
    
    println("10D array:");
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 1; j++) {
            for (int k = 0; k < 1; k++) {
                for (int l = 0; l < 1; l++) {
                    for (int m = 0; m < 1; m++) {
                        for (int n = 0; n < 1; n++) {
                            for (int o = 0; o < 1; o++) {
                                for (int p = 0; p < 1; p++) {
                                    for (int q = 0; q < 1; q++) {
                                        for (int r = 0; r < 2; r++) {
                                            println(arr10d[i][j][k][l][m][n][o][p][q][r]);
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
    
    // 6次元配列のより複雑な例 int[2][2][1][2][1][3]
    int[2][2][1][2][1][3] arr6d = [
        [  // レベル1の1番目要素
            [  // レベル2の1番目要素  
                [  // レベル3の1番目要素
                    [[1, 2, 3]],  // レベル4の1番目要素
                    [[4, 5, 6]]   // レベル4の2番目要素
                ]
            ],
            [  // レベル2の2番目要素
                [  // レベル3の1番目要素
                    [[7, 8, 9]],   // レベル4の1番目要素
                    [[10, 11, 12]] // レベル4の2番目要素
                ]
            ]
        ], 
        [  // レベル1の2番目要素
            [  // レベル2の1番目要素
                [  // レベル3の1番目要素
                    [[13, 14, 15]], // レベル4の1番目要素
                    [[16, 17, 18]]  // レベル4の2番目要素
                ]
            ],
            [  // レベル2の2番目要素
                [  // レベル3の1番目要素
                    [[19, 20, 21]], // レベル4の1番目要素
                    [[22, 23, 24]]  // レベル4の2番目要素
                ]
            ]
        ]
    ];
    
    println("6D array:");
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 2; j++) {
            for (int k = 0; k < 1; k++) {
                for (int l = 0; l < 2; l++) {
                    for (int m = 0; m < 1; m++) {
                        for (int n = 0; n < 3; n++) {
                            println(arr6d[i][j][k][l][m][n]);
                        }
                    }
                }
            }
        }
    }
}
