int main() {
    int a[3];
    for(tiny i = 0;i < 3;i++){
        a[i] = i + 1;
    }
    // 範囲外アクセス（下限）
    // print(a[-1]); // エラーになるべき
    // 範囲外アクセス（上限）
    // print(a[3]);  // エラーになるべき
    // 正常アクセス
    print(a[0]); // 1
    print(a[2]); // 3
}
