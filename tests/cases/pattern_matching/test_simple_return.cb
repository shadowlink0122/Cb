// シンプルな関数返り値テスト

enum Result<T, E> {
    Ok(T),
    Err(E)
};

Result<int, int> make_ok() {
    Result<int, int> r = Result<int, int>::Ok(42);
    return r;
}

int main() {
    println("Test start");
    
    // 直接代入
    Result<int, int> r1 = make_ok();
    println("After assignment");
    
    return 0;
}
