struct A { int x; };
struct B { A* ptr; };
struct C { B* ptr; };
struct D { C* ptr; };

int main(){
    A a = { 99 };
    B b = { &a };
    C c = { &b };
    D d = { &c };
    
    // 3 consecutive arrows, then simple member
    int result = d.ptr->ptr->ptr->x;
    return result;
}
