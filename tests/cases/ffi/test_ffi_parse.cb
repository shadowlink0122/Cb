// v0.13.0 FFI パーサーテスト
// 外部モジュール宣言が正しくパースされることを確認

use foreign.m {
    double sqrt(double x);
    double pow(double x, double y);
    double sin(double x);
}

void main() {
    println("FFI parser test - declarations parsed successfully");
}
