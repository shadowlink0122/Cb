enum Priority {
    LOW = 1,
    MEDIUM = 2,
    HIGH = 3
};

struct Task {
    int id;
    Priority priority;
};

int main() {
    // Test 1: Direct enum access
    Priority p1 = Priority::LOW;
    println("Direct enum:", p1);
    
    // Test 2: Struct initialization
    Task task = {1, 1};
    println("Struct member:", task.priority);
    
    // Test 3: Enum assignment
    Priority p2 = 2;
    println("Assignment:", p2);
    
    return 0;
}
