# v0.14.0: 未確認挙動とテスト不足の改善計画

**作成日**: 2025年11月11日  
**対象バージョン**: v0.14.0  
**目的**: v0.13.0で実装された機能の包括的テストと、未実装機能の文書化

---

## 📋 概要

v0.13.0では以下の機能が実装されました：
- ✅ async/await基本機能
- ✅ Generic Interface
- ✅ Nested Generic Types (Option<Result<T, E> >)
- ✅ Direct return enum variants

しかし、以下の問題が発見されました：
1. **Integration Testに含まれていないテストが19個**（52個中35個のみカバー）
2. **未実装の機能組み合わせが4つ**
3. **動作未確認の機能が複数**

---

## 🔴 未実装機能（v0.14.0で実装予定）

### 1. ❌ Async関数を引数に取る

**現状**: パースエラー

```cb
async int compute() {
    return 42;
}

void executor(async int* callback) {  // ❌ エラー
    int result = await callback();
    println("Result: {result}");
}

void main() {
    executor(compute);
}
```

**エラー**:
```
error: Expected type specifier
void executor(async int* callback) {
              ^
```

**必要な実装**:
- [ ] async関数型のパース対応
- [ ] async関数型の型システムサポート
- [ ] 関数ポインタとしてのasync関数
- [ ] テストケース作成

---

### 2. ❌ Asyncラムダ式

**現状**: パースエラー

```cb
void main() {
    auto lambda = async int func {  // ❌ エラー
        return 42;
    };
    int result = await lambda();
    println("Result: {result}");
}
```

**エラー**:
```
error: Unexpected token
auto lambda = async int func {
              ^
```

**必要な実装**:
- [ ] asyncキーワード + ラムダ式のパース
- [ ] async lambdaの型推論
- [ ] async lambdaのコード生成
- [ ] テストケース作成

---

### 4. ❌ ジェネリクス構造体の配列サポート

**現状**: 宣言は可能だが、配列要素へのアクセス時に型情報が失われる

```cb
Future<int>[3] futures;  // 宣言は可能
futures[0] = task_id(1);
int r0 = await futures[0];  // ❌ エラー: await expression requires Future<T> operand
```

**回避策**: 配列を使わず、個別の変数を使用
```cb
Future<int> f0 = task_id(1);
Future<int> f1 = task_id(2);
int r0 = await f0;  // ✅ 動作する
int r1 = await f1;
```

**必要な実装**:
- [ ] 配列アクセス式での型情報保持
- [ ] AST_ARRAY_REFの明示的な処理
- [ ] ジェネリクス構造体配列の変数登録
- [ ] テストケース作成

**詳細**: `docs/todo/v0.14.0_generic_array_support.md`

---

## ✅ 実装済み機能（v0.12.0~v0.13.0）

以下の機能は既に実装済みで、正常に動作しています：

### 1. ✅ Timeout機能（v0.12.1）
- timeout() builtin関数
- Event loopでのタイムアウトチェック
- Integration test完備

### 2. ✅ ?オペレーター（v0.12.1）
- Result<T, E>とOption<T>のエラー伝播
- Integration test完備

### 3. ✅ Async Generic Interface（v0.13.0）
- `interface AsyncProcessor<T>` などのジェネリックインターフェース
- `impl AsyncProcessor<int> for Processor` 構文
- 単一および複数型パラメータ対応

### 4. ✅ Generic Interface impl構文（v0.12.0）
- `impl Container<T> for Box` 構文
- 具体的な型での実装
- async メソッドとの組み合わせ

---

## 🟡 動作未確認機能（v0.14.0でテスト追加予定）

### 1. ⚠️ Async + 複数ジェネリック型パラメータ

**現状**: パースは通るが、統合テスト未実施

```cb
async Result<T, E> compute<T, E>(T value, E error_val) {
    if (value > 0) {
        return Result<T, E>::Ok(value);
    } else {
        return Result<T, E>::Err(error_val);
    }
}

void main() {
    Result<int, string> result = await compute<int, string>(42, "error");
    println("Got result");
}
```

**必要なテスト**:
- [ ] 基本動作テスト
- [ ] 複数の型パラメータ組み合わせ
- [ ] エラーハンドリング
- [ ] Integration test追加

---

### 2. ⚠️ ネストしたMatch式（2段階以上）

**現状**: 1段階のmatchは動作するが、ネストは未サポート

```cb
// ✅ 動作する（1段階）
match (some_ok) {
    Some(res) => {
        println("Got Some");
    }
    None => {
        println("Got None");
    }
}

// ❌ 動作しない（2段階）
match (some_ok) {
    Some(res) => {
        match (res) {  // ❌ エラー: Not an enum type
            Ok(v) => { println("Ok: {v}"); }
            Err(e) => { println("Err: {e}"); }
        }
    }
    None => {
        println("Got None");
    }
}
```

**問題**: match内の変数がenum情報を保持していない

**必要な実装**:
- [ ] match内の変数に型情報を保持
- [ ] ネストしたmatchのサポート
- [ ] テストケース作成

---

### 3. ✅ Async Generic Interface実装（実装済み - v0.13.0）

**現状**: **実装済みで正常動作**

**テストファイル**:
- ✅ `tests/cases/async/test_async_generic_interface_comprehensive.cb` - 動作確認済み
- ✅ 単一型パラメータ: `interface AsyncProcessor<T>`
- ✅ 複数型パラメータ: `interface AsyncConverter<T, U>`
- ✅ async Result<T, E>との組み合わせ

**ジェネリックimpl構文の仕様**:

Cb言語では、`impl<T>` という明示的な型パラメータ宣言は不要です。型パラメータはinterfaceとstructの定義から自動的に推論されます。

```cb
// ✅ 正しい構文
interface Container<T> {
    async T get_value();
}

struct Box<T> {
    T value;
}

impl Container<T> for Box<T> {  // impl<T>は不要
    async T get_value() {
        return self.value;
    }
}
```

**必要な作業**:
- [ ] Integration testに追加（優先度: 中）

---

### 5. ⚠️ 深いネストジェネリック型（3段階以上）

**現状**: 2段階（Option<Result<T, E> >）は動作確認済み

```cb
// ⚠️ 未確認
Option<Result<Future<int>, string> > deep_nested = ...;
Future<Option<Result<int, string> > > another_pattern = ...;
```

**必要なテスト**:
- [ ] 3段階ネスト
- [ ] 4段階以上のネスト
- [ ] パフォーマンステスト
- [ ] メモリリークチェック

---

### 6. ⚠️ Asyncメソッドの再帰呼び出し

**現状**: 基本的な再帰は動作するが、複雑な再帰は未確認

```cb
// ⚠️ 未確認
async int fibonacci(int n) {
    if (n <= 1) {
        return n;
    }
    int a = await fibonacci(n - 1);
    int b = await fibonacci(n - 2);
    return a + b;
}
```

**必要なテスト**:
- [ ] async再帰の深度テスト
- [ ] スタックオーバーフロー対策
- [ ] 相互再帰（mutual recursion）
- [ ] Integration test追加

---

### 7. ⚠️ 例外安全性（Error handling）

**現状**: 基本的なResult型は動作するが、例外的ケースは未確認

```cb
// ⚠️ 未確認
async Result<int, string> risky_operation() {
    // パニックやクラッシュ時の挙動
    // メモリリーク防止
    // リソースクリーンアップ
}
```

**必要なテスト**:
- [ ] エラー時のリソースクリーンアップ
- [ ] 未awaitのFutureの破棄
- [ ] タスクキューのクリーンアップ
- [ ] メモリリークテスト

---

## 📊 Integration Testカバレッジ不足

### 現状

```
Total test files: 52
Integration tests: 35
Missing: 17 tests (32.7%)
```

### 漏れているテスト（19個）

1. ❌ `test_async_complex_patterns.cb`
2. ❌ `test_async_cooperative.cb`
3. ❌ `test_async_generic_interface_comprehensive.cb` ⚠️ **重要**
4. ❌ `test_async_loop.cb`
5. ❌ `test_async_nested.cb`
6. ❌ `test_async_recursive.cb` ⚠️ **重要**
7. ❌ `test_async_result_basic.cb`
8. ❌ `test_async_result_propagation.cb`
9. ❌ `test_async_simple_result.cb`
10. ❌ `test_async_sleep.cb`
11. ❌ `test_async_variant_check.cb`
12. ❌ `test_nested_generic_non_async.cb`
13. ❌ `test_option_async_integration.cb`
14. ❌ `test_result_only.cb`
15. ❌ `test_task_queue_basic.cb` ⚠️ **重要**
16. ❌ `test_task_queue_comprehensive.cb` ⚠️ **重要**
17. ❌ `test_task_queue.cb` ⚠️ **重要**
18. ❌ `test_yield_basic.cb`
19. ❌ `test_yield_edge_cases.cb` ⚠️ **重要**

---

## 🎯 v0.14.0 実装計画

### Phase 1: Integration Testカバレッジ改善（優先度: 高）

**目標**: 全52テストをIntegration testに追加

**タスク**:
1. [ ] 19個の漏れているテストをtest_async.hppに追加
2. [ ] 各テストの期待出力を確認
3. [ ] アサーションを追加
4. [ ] 全テスト実行確認

**期待される効果**:
- テストカバレッジ: 67% → 100%
- バグの早期発見
- リグレッション防止

---

### Phase 2: 未実装機能の実装（優先度: 中）

#### 2.1 Async関数型のサポート

**実装内容**:
```cb
// 関数型としてのasync
typedef async int AsyncFunction;

void executor(AsyncFunction* callback) {
    int result = await callback();
}

async int compute() {
    return 42;
}

void main() {
    executor(&compute);
}
```

**実装タスク**:
1. [ ] async関数型のパース
2. [ ] 型システムへの統合
3. [ ] コード生成
4. [ ] テストケース作成（10+）
5. [ ] Integration test追加

---

#### 2.2 Asyncラムダ式のサポート

**実装内容**:
```cb
void main() {
    auto lambda = async int(int x) {
        yield;
        return x * 2;
    };
    
    int result = await lambda(21);
    println("Result: {result}");
}
```

**実装タスク**:
1. [ ] async lambda構文のパース
2. [ ] 型推論（auto + async lambda）
3. [ ] キャプチャ対応
4. [ ] コード生成
5. [ ] テストケース作成（15+）
6. [ ] Integration test追加

---

#### 2.3 構造体内asyncメソッドのサポート

**実装内容**:
```cb
struct Calculator<T> {
    T base;
    
    async T compute(T value) {
        yield;
        return this->base + value;
    }
}
```

**実装タスク**:
1. [ ] 構造体定義内でのasync修飾子パース
2. [ ] ジェネリック型パラメータとの統合
3. [ ] thisポインタのキャプチャ
4. [ ] コード生成
5. [ ] テストケース作成（10+）
6. [ ] Integration test追加

---

### Phase 3: 動作未確認機能のテスト追加（優先度: 中）

#### 3.1 包括的なAsync+Genericsテスト

**テストケース**:
1. [ ] Async + 複数ジェネリック型パラメータ
2. [ ] Async + ネストジェネリック型
3. [ ] Async + 制約付きジェネリック型
4. [ ] Async + デフォルト型パラメータ

**期待される成果物**:
- `test_async_multi_generic_comprehensive.cb`
- Integration test追加
- ドキュメント更新

---

#### 3.2 ネストしたMatch式のサポート

**実装内容**:
```cb
match (outer) {
    Some(inner) => {
        match (inner) {  // 2段階match
            Ok(value) => { println("Value: {value}"); }
            Err(err) => { println("Error: {err}"); }
        }
    }
    None => { println("None"); }
}
```

**実装タスク**:
1. [ ] match内の変数に型情報を保持
2. [ ] ネストスコープの管理
3. [ ] コード生成
4. [ ] テストケース作成（20+）
5. [ ] Integration test追加

---

#### 3.3 深いネストジェネリック型のテスト

**テストケース**:
1. [ ] 3段階ネスト: `Option<Result<Future<T>, E> >`
2. [ ] 4段階ネスト: `Future<Option<Result<Box<T>, E> > >`
3. [ ] 混合ネスト: `Result<Option<T>, Option<E> >`
4. [ ] パフォーマンステスト

**期待される成果物**:
- `test_deep_nested_generics.cb`
- パフォーマンスベンチマーク
- メモリリークテスト

---

#### 3.4 Async再帰のテスト

**テストケース**:
1. [ ] 単純な再帰（fibonacci）
2. [ ] 相互再帰（mutual recursion）
3. [ ] 深い再帰（1000+段階）
4. [ ] スタックオーバーフロー対策

**期待される成果物**:
- `test_async_recursion_comprehensive.cb`
- スタック使用量測定
- Integration test追加

---

#### 3.5 例外安全性とリソース管理のテスト

**テストケース**:
1. [ ] 未awaitのFuture破棄
2. [ ] タスクキューのクリーンアップ
3. [ ] メモリリーク検出
4. [ ] リソースリークテスト（ファイル、ソケット等）

**期待される成果物**:
- `test_async_resource_safety.cb`
- AddressSanitizer統合
- LeakSanitizer統合
- Valgrindテスト

---

### Phase 4: ドキュメント整備（優先度: 低）

**タスク**:
1. [ ] BNF更新（async lambda, async function type）
2. [ ] 言語仕様書更新
3. [ ] チュートリアル作成
4. [ ] ベストプラクティスガイド作成
5. [ ] エラーメッセージ改善

---

## 📈 成功指標

### テストカバレッジ

| メトリクス | 現在 | v0.14.0目標 |
|-----------|------|------------|
| Integration test coverage | 67% | 100% |
| Unit test coverage | 85% | 95% |
| Feature coverage | 75% | 95% |
| Edge case coverage | 60% | 90% |

### 実装完了度

| 機能 | 現在 | v0.14.0目標 |
|-----|------|------------|
| Async function type | ❌ 0% | ✅ 100% |
| Async lambda | ❌ 0% | ✅ 100% |
| Struct async method | ❌ 0% | ✅ 100% |
| Generic impl syntax | ❌ 0% | ✅ 100% |
| Nested match | ❌ 0% | ✅ 100% |

---

## 🔄 実装優先順位

### 優先度 S（必須）
1. ✅ Integration testカバレッジ100%達成（19テスト追加）
2. ✅ Async function typeサポート
3. ✅ 重要なバグ修正

### 優先度 A（重要）
1. Asyncラムダ式サポート
3. ネストしたmatchサポート

### 優先度 B（推奨）
1. 深いネストジェネリック型テスト
2. Async再帰の包括的テスト
3. 例外安全性テスト

### 優先度 C（任意）
1. パフォーマンス最適化
2. ドキュメント整備
3. エラーメッセージ改善

---

## 📝 備考

### 既知の制限事項

1. **>> トークン問題**: ネストジェネリック型は`>>`スペースなしでも実装可能にする
2. **Match内の型情報**: 現在は1段階のみサポート
3. **Async関数型**: 未サポート（v0.13.*で実装予定）

### 互換性

v0.14.0は**後方互換性を維持**します。既存のv0.13.0コードは全て動作します。

---

**作成者**: Cb Language Team  
**最終更新**: 2025年11月11日  
**ステータス**: Draft → v0.14.0実装予定
