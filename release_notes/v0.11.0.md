# Cb言語 v0.11.0 リリースノート

## リリース日
2025年10月27日

## 概要
v0.11.0では、**ジェネリクスシステム（Generics）**と**文字列補間機能（String Interpolation）**を実装しました。型パラメータによる汎用的なコードの記述が可能になり、文字列リテラル内で変数や式を直接評価できるようになりました。これにより、コードの再利用性と可読性が大幅に向上しました。

**注**: v0.11.0は複数のパートに分かれて実装されます。本リリースはPart 1（基盤機能）であり、Part 2では非同期処理（async/await）、Result型、パターンマッチングを実装予定です。

### v0.11.0の全体構成

#### Part 1: 基盤機能（✅ 2025年10月27日リリース）
- ジェネリクスシステム（構造体、関数、列挙型）
- 文字列補間機能
- Option型とResult型（基本定義）

#### Part 2: 非同期処理（🔵 実装中 - 2026年1月リリース予定）
- Event Loop
- Future<T>型
- Result<T, E>型の完全実装
- パターンマッチング
- async/await構文
- ?オペレーター

詳細は `docs/todo/v0.11.0_implementation_roadmap.md` を参照してください。

---

## ✨ 新機能

### 1. 🎯 ジェネリクスシステム

**概要**: 型パラメータによる汎用的な構造体・関数・列挙型の定義が可能に

#### 基本構文

##### ジェネリック構造体
```cb
struct Box<T> {
    T value;
}

void main() {
    Box<int> int_box;
    int_box.value = 42;
    
    Box<string> str_box;
    str_box.value = "Hello, Generics!";
    
    println(int_box.value);  // 42
    println(str_box.value);  // Hello, Generics!
}
```

##### ジェネリック関数
```cb
fn swap<T>(T a, T b) -> void {
    T temp = a;
    a = b;
    b = temp;
}

void main() {
    int x = 10;
    int y = 20;
    swap<int>(x, y);
    println(x, ", ", y);  // 20, 10
}
```

##### ジェネリック列挙型
```cb
enum Option<T> {
    Some(T),
    None
}

enum Result<T, E> {
    Ok(T),
    Err(E)
}

void main() {
    Option<int> some_value = Some(42);
    Option<int> no_value = None;
    
    Result<int, string> success = Ok(100);
    Result<int, string> failure = Err("Error occurred");
}
```

#### サポート機能

##### 複数型パラメータ
```cb
struct Pair<T, U> {
    T first;
    U second;
}

void main() {
    Pair<int, string> p;
    p.first = 1;
    p.second = "one";
}
```

##### ネストしたジェネリック型
```cb
struct Box<T> {
    T value;
}

void main() {
    Box<Option<int>> nested;
    nested.value = Some(42);
    
    Box<Result<string, int>> complex;
    complex.value = Ok("success");
}
```

##### 配列型のサポート
```cb
struct Container<T> {
    T items;
}

void main() {
    Container<int[10]> array_container;
    array_container.items[0] = 100;
}
```

##### ポインタ型のサポート
```cb
struct Node<T> {
    T data;
    Node<T>* next;
}

void main() {
    Node<int> node1;
    node1.data = 10;
    node1.next = nullptr;
    
    Node<string> node2;
    node2.data = "Hello";
    node2.next = nullptr;
}
```

##### 再帰的構造体
```cb
struct LinkedList<T> {
    T value;
    LinkedList<T>* next;
}

void main() {
    LinkedList<int> list;
    list.value = 1;
    list.next = nullptr;
}
```

#### 新しく利用可能な型

##### Option型
```cb
// 値の有無を表現
Option<int> find_value(int target) {
    if (found) {
        return Some(target);
    } else {
        return None;
    }
}

void main() {
    Option<int> result = find_value(42);
    
    // パターンマッチング（将来実装予定）
    match result {
        Some(value) => println("Found: ", value),
        None => println("Not found")
    }
}
```

##### Result型
```cb
// 成功/失敗を表現
Result<int, string> divide(int a, int b) {
    if (b == 0) {
        return Err("Division by zero");
    } else {
        return Ok(a / b);
    }
}

void main() {
    Result<int, string> result = divide(10, 2);
    
    // エラーハンドリング（将来実装予定）
    match result {
        Ok(value) => println("Result: ", value),
        Err(error) => println("Error: ", error)
    }
}
```

---

### 2. 📝 文字列補間機能

**概要**: Python/C#スタイルの文字列補間により、変数や式を文字列中に直接埋め込み可能に

#### 基本構文

```cb
void main() {
    string name = "Alice";
    int age = 25;
    
    // 変数の埋め込み
    string msg = "Name: {name}, Age: {age}";
    println(msg);  // Name: Alice, Age: 25
}
```

#### サポートされる式

##### 算術演算
```cb
void main() {
    int a = 10;
    int b = 20;
    
    string result = "Sum: {a + b}, Product: {a * b}";
    println(result);  // Sum: 30, Product: 200
}
```

##### 配列アクセス
```cb
void main() {
    int[3] numbers = {10, 20, 30};
    
    string msg = "First: {numbers[0]}, Second: {numbers[1]}";
    println(msg);  // First: 10, Second: 20
}
```

##### メンバーアクセス
```cb
struct Point {
    int x;
    int y;
}

void main() {
    Point p;
    p.x = 100;
    p.y = 200;
    
    string msg = "Point: ({p.x}, {p.y})";
    println(msg);  // Point: (100, 200)
}
```

#### フォーマット指定子

##### 16進数表示
```cb
void main() {
    int value = 255;
    
    string hex_lower = "Hex: {value:x}";  // Hex: ff
    string hex_upper = "Hex: {value:X}";  // Hex: FF
    
    println(hex_lower);
    println(hex_upper);
}
```

##### 小数点以下の桁数指定
```cb
void main() {
    double pi = 3.14159265;
    
    string two_digits = "Pi: {pi:.2}";   // Pi: 3.14
    string four_digits = "Pi: {pi:.4}";  // Pi: 3.1416
    
    println(two_digits);
    println(four_digits);
}
```

##### ゼロパディング
```cb
void main() {
    int number = 42;
    
    string padded = "Number: {number:05}";  // Number: 00042
    println(padded);
}
```

##### 2進数表示
```cb
void main() {
    int value = 15;
    
    string binary = "Binary: {value:b}";  // Binary: 1111
    println(binary);
}
```

#### エスケープシーケンス

```cb
void main() {
    int value = 100;
    
    // {{ と }} でエスケープ
    string msg = "Value: {{value}} = {value}";
    println(msg);  // Value: {value} = 100
}
```

---

## 🔧 実装詳細

### ジェネリクス実装

#### フロントエンド
1. **Lexer** (`src/frontend/recursive_parser/recursive_lexer.cpp`)
   - `<`, `>` トークンの文脈依存認識

2. **Parser** (`src/frontend/recursive_parser/recursive_parser.cpp`)
   - `parseGenericParams()`: 型パラメータ `<T>`, `<T, U>` の解析
   - `parseGenericArgs()`: 型引数 `Box<int>` の解析
   - ネストしたジェネリック型の再帰的パース

3. **Type Utility Parser** (`src/frontend/recursive_parser/parsers/type_utility_parser.cpp`)
   - `parse_type()`: ジェネリック型情報の構築
   - 配列型・ポインタ型との組み合わせ

4. **AST** (`src/common/ast.h`)
   - `generic_params`: 型パラメータリスト
   - `generic_args`: 型引数リスト
   - `ParsedTypeInfo`: ジェネリック型情報を保持

#### バックエンド
1. **Interpreter** (`src/backend/interpreter/core/interpreter.cpp`)
   - ジェネリック型の実体化（instantiation）
   - 型パラメータから具体型への置換
   - インスタンスキャッシュの管理

2. **Generic Instantiation** (`src/backend/interpreter/evaluator/functions/generic_instantiation.cpp`) - 新規ファイル
   - `instantiate_generic_struct()`: 構造体の実体化
   - `instantiate_generic_function()`: 関数の実体化
   - 型パラメータのマッピングと再帰的置換

3. **Variable Declaration** (`src/backend/interpreter/managers/variables/declaration.cpp`)
   - ジェネリック型変数の宣言処理
   - 型パラメータの解決

4. **Function Call** (`src/backend/interpreter/evaluator/functions/call_impl.cpp`)
   - ジェネリック関数呼び出し時の型推論
   - 実体化されたインスタンスの選択

### 文字列補間実装

#### フロントエンド
1. **Lexer** (`src/frontend/recursive_parser/recursive_lexer.cpp`)
   - `makeString()`: 文字列中の `{...}` パターン検出
   - `TOK_INTERPOLATED_STRING` トークン生成

2. **Parser** (`src/frontend/recursive_parser/parsers/primary_expression_parser.cpp`)
   - `parseInterpolatedString()`: 文字列補間の解析
   - テキストセグメントと式セグメントの分離
   - フォーマット指定子（`:x`, `:.2` など）の抽出

3. **AST** (`src/common/ast.h`)
   - `AST_INTERPOLATED_STRING`: 補間文字列ノード
   - `AST_STRING_INTERPOLATION_SEGMENT`: セグメントノード
   - `interpolation_segments`: セグメントリスト
   - `interpolation_format`: フォーマット指定子

#### バックエンド
1. **Evaluator** (`src/backend/interpreter/evaluator/core/evaluator.cpp`)
   - `evaluate_interpolated_string()`: 文字列補間の評価
   - `format_interpolated_value()`: フォーマット指定子の処理
   - 16進数、2進数、小数点精度、ゼロパディングのサポート

2. **Expression Evaluator** (`src/backend/interpreter/evaluator/core/dispatcher.cpp`)
   - `AST_INTERPOLATED_STRING` ケースの処理
   - セグメント単位での式評価と連結

---

## 📊 テスト結果

### 統計
- **統合テスト**: 3,181個（3,179個成功、2個既存の問題）
- **ユニットテスト**: 30個（100%成功）
- **新規ジェネリクステスト**: 37ケース（100%成功）
- **新規文字列補間テスト**: 13ケース（80アサーション、100%成功）
- **総テスト数**: 3,211個

### 新規追加テスト

#### ジェネリクステスト (`tests/cases/generics/`)
1. **basic_struct.cb** - 基本的な構造体ジェネリクス
2. **multiple_params.cb** - 複数型パラメータ
3. **nested_generic_comprehensive.cb** - ネストしたジェネリック型
4. **function_comprehensive.cb** - 関数ジェネリクスの包括的テスト
5. **enum_comprehensive.cb** - 列挙型ジェネリクス
6. **array_type.cb** - 配列型のサポート
7. **pointer_type.cb** - ポインタ型のサポート
8. **recursive_struct.cb** - 再帰的構造体
9. **result_type.cb** - Result型の動作確認
10. **struct_as_function_param.cb** - ジェネリック構造体を関数パラメータとして使用

**その他27ファイル**

#### 文字列補間テスト (`tests/cases/string_interpolation/`)
1. **test_basic.cb** - 基本的な変数埋め込み
2. **test_expressions.cb** - 算術演算の評価
3. **test_array_access.cb** - 配列要素アクセス
4. **test_member_access.cb** - 構造体メンバーアクセス
5. **test_types.cb** - 型変換
6. **test_edge_cases.cb** - エッジケース
7. **test_escape.cb** - エスケープシーケンス
8. **format_specifiers.cb** - フォーマット指定子
9. **basic_interpolation.cb** - 包括的な統合テスト

**その他4ファイル**

#### 統合テスト
1. **tests/integration/generics/test_generics.hpp**
   - 37個のジェネリクステストケースを実行
   - 構造体、関数、enum、ネスト型、配列、ポインタのテスト

2. **tests/integration/string_interpolation/test_string_interpolation.hpp**
   - 13個の文字列補間テストケースを実行
   - 基本機能、式評価、フォーマット指定子のテスト

---

## 🐛 バグ修正

### ネスト構造体のコンストラクタ実行後の同期
**コミット**: `cf91c67`

**問題**: ネスト構造体のコンストラクタで初期化したメンバーが外側の構造体に反映されない

**修正内容**: 
- コンストラクタ実行後に親構造体のメンバーを同期
- `sync_nested_struct_members()` の改善

### ジェネリック構造体を関数パラメータとして使用
**コミット**: `e18530c`

**問題**: `Box<int>` などのジェネリック型を関数引数として渡せない

**修正内容**:
- 関数呼び出し時のジェネリック型解決を改善
- 型パラメータのマッピング処理を修正

---

## 📚 ドキュメント

### 新規作成

1. **docs/CODING_GUIDELINES.md** (553行)
   - テストの作成手順（必読）
   - コーディング規約
   - コミット規約
   - 開発ワークフロー

2. **docs/features/string_interpolation.md** (580行)
   - 文字列補間の完全な仕様
   - 構文定義
   - フォーマット指定子の詳細
   - 実装ノート

3. **docs/features/string_interpolation_examples.md** (269行)
   - 実用的な使用例
   - パターン集

4. **docs/features/enum_with_associated_values.md** (63行)
   - 関連値付きenumの仕様

5. **docs/todo/v0.11.0_generics_minimal_spec.md** (440行)
   - ジェネリクスの最小仕様

### 更新

1. **README.md**
   - CODING_GUIDELINES.md へのリンク追加
   - v0.11.0 機能の概要追加

2. **docs/README.md**
   - CODING_GUIDELINES.md を最初のエントリとして追加
   - 必読ドキュメントとして強調

### ドキュメント整理

- **docs/features/** に機能関連ドキュメントを集約
  - `default_arguments_implementation_report.md`
  - `DEFAULT_ARGUMENTS_SUMMARY.md`
  - `switch_comprehensive_test_report.md`
  - `switch_implementation_report.md`
  - `string_interpolation*.md`

- **docs/fixes/** に修正関連ドキュメントを移動
  - `struct_array_loop_member_access_fix.md`

---

## 🎯 使用例

### ジェネリクスの実用例

#### スタックの実装
```cb
struct Stack<T> {
    T[100] items;
    int top;
}

impl Stack<T> {
    self() {
        self.top = -1;
    }
    
    void push(T item) {
        self.top = self.top + 1;
        self.items[self.top] = item;
    }
    
    T pop() {
        T item = self.items[self.top];
        self.top = self.top - 1;
        return item;
    }
    
    bool is_empty() {
        return self.top == -1;
    }
}

void main() {
    Stack<int> int_stack;
    int_stack.push(10);
    int_stack.push(20);
    println(int_stack.pop());  // 20
    
    Stack<string> str_stack;
    str_stack.push("Hello");
    str_stack.push("World");
    println(str_stack.pop());  // World
}
```

#### 安全なNull許容型
```cb
Option<T> find<T>(T[100] array, int size, T target) {
    for (int i = 0; i < size; i = i + 1) {
        if (array[i] == target) {
            return Some(array[i]);
        }
    }
    return None;
}

void main() {
    int[5] numbers = {1, 2, 3, 4, 5};
    
    Option<int> result = find<int>(numbers, 5, 3);
    // result は Some(3)
    
    Option<int> not_found = find<int>(numbers, 5, 10);
    // not_found は None
}
```

### 文字列補間の実用例

#### ログ出力
```cb
void log_info(string message, int line, string file) {
    string timestamp = get_timestamp();
    string log = "[{timestamp}] INFO [{file}:{line}] {message}";
    println(log);
}

void main() {
    log_info("User logged in", 42, "main.cb");
    // [2025-10-27 10:30:00] INFO [main.cb:42] User logged in
}
```

#### デバッグ出力
```cb
void debug_point(Point p, string name) {
    string debug = "Point {name} = ({p.x}, {p.y})";
    println(debug);
}

void main() {
    Point origin;
    origin.x = 0;
    origin.y = 0;
    debug_point(origin, "origin");  // Point origin = (0, 0)
}
```

#### 数値フォーマット
```cb
void print_statistics(int count, double average, double min, double max) {
    string stats = "Statistics:\n" +
                   "  Count: {count}\n" +
                   "  Average: {average:.2}\n" +
                   "  Min: {min:.2}\n" +
                   "  Max: {max:.2}";
    println(stats);
}
```

---

## 🔄 互換性

### 後方互換性
- ✅ 既存のコードに影響なし
- ✅ ジェネリクスはオプトイン（使用したい場合のみ型パラメータを使用）
- ✅ 文字列補間は `{}` を含む文字列でのみ有効
- ✅ 通常の文字列リテラルは従来通り動作

### C++との互換性
| 機能 | Cb | C++ |
|-----|-----|-----|
| ジェネリクス構文 | `struct Box<T>` | `template<typename T> struct Box` |
| 関数テンプレート | `fn swap<T>` | `template<typename T> void swap` |
| 文字列補間 | `{variable}` | `std::format("{}", variable)` (C++20) |

---

## ⚠️ 制限事項と既知の問題

### ジェネリクス
1. **型制約（trait bounds）は未実装**
   - すべての型がジェネリック型パラメータとして使用可能
   - 特定のインターフェースを実装している型のみに制限する機能は未実装

2. **デフォルト型パラメータは未実装**
   ```cb
   // ❌ 未対応
   struct Box<T = int> { T value; }
   ```

3. **可変長型パラメータは未実装**
   ```cb
   // ❌ 未対応
   struct Tuple<T...> { /* ... */ }
   ```

### 文字列補間
1. **フォーマット指定子は数値型のみ**
   - 文字列型には適用不可
   - カスタムフォーマットは未サポート

2. **パフォーマンス**
   - 実行時に式を評価するため、静的な文字列結合よりオーバーヘッドあり
   - 通常の使用では問題なし

3. **ネストした補間は未対応**
   ```cb
   // ❌ 未対応
   string nested = "Value: {{"inner {value}"}}";
   ```

---

## 📈 パフォーマンス

### ジェネリクス
- **実体化（Instantiation）**: コンパイル時ではなく実行時
- **インスタンスキャッシュ**: 同じ型引数での再実体化を回避
- **オーバーヘッド**: 初回実体化時のみ、以降はキャッシュから取得

### 文字列補間
- **式評価**: 実行時に評価
- **文字列連結**: 標準ライブラリの文字列操作を使用
- **フォーマット処理**: `<iomanip>`, `<sstream>` を使用

---

## 🔜 v0.11.0 Part 2 計画（2026年1月予定）

### 非同期処理とエラーハンドリング

v0.11.0 Part 2では、Part 1で実装したジェネリクスを基盤として、非同期処理とエラーハンドリングを実装します。

#### 主要機能
1. **Event Loop** - 非同期タスクのスケジューリング
2. **Future<T>型** - 非同期処理の結果を表現
3. **Result<T, E>型の完全実装** - パターンマッチングとの統合
4. **パターンマッチング** - `match` 式によるEnumの分岐
5. **async/await構文** - TypeScript/Rust風の非同期構文
6. **?オペレーター** - エラー伝播の簡潔な記法

#### 実装スケジュール
- **Phase 1**: Event Loop + タイマー（2週間）
- **Phase 2**: Future<T>型（1週間）
- **Phase 3**: Result<T, E> + パターンマッチング（3週間）
- **Phase 4**: async/await構文（3週間）
- **Phase 5**: ?オペレーター（1週間）
- **Phase 6**: 並行実行 + ライブラリ（2週間）

**リリース目標**: 2026年1月19日

詳細は `docs/todo/v0.11.0_implementation_roadmap.md` を参照してください。

---

## 🔜 v0.12.0 以降の計画

### v0.12.0（主要機能）
1. **ムーブセマンティクスの完全実装**（v0.10.1から繰り越し）
   - return文での自動ムーブ（NRVO）
   - ムーブ代入演算子
   - 右辺値の自動検出

2. **文字列補間の最適化**
   - 静的文字列の事前評価
   - フォーマット処理の高速化

3. **エラーメッセージの改善**
   - ジェネリック型の型不一致エラー
   - 文字列補間の構文エラー

### v0.13.0以降
1. **トレイト（Trait）システム**
   - ジェネリック型の制約
   - インターフェースの拡張

2. **パターンマッチング** - TypeScript/Rust風の非同期構文

3. **非同期/await構文**
   - 基本的な非同期処理
   - async/await キーワード

---

## 📝 変更履歴

### コミット統計
- **変更ファイル数**: 103ファイル
- **追加行数**: +11,635行
- **削除行数**: -288行
- **純増**: +11,347行

### 主要なコミット
- `22468ef` - feat(v0.11.0): Implement comprehensive generics system
- `e18530c` - fix(generics): Support generic structs as function parameters
- `cf91c67` - fix: Sync nested struct members after constructor execution
- `1da0122` - feat(string_interpolation): Add string interpolation feature (v0.11.0)

---

## 🙏 謝辞

このリリースに貢献してくださったすべての方々に感謝します。

---

**リリース承認**: shadowlink0122  
**レビュー**: AI Assistant  
**日付**: 2025年10月27日
