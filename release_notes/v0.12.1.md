# Cb言語 v0.12.1 リリースノート

**リリース日**: 2025年11月12日  
**バージョン**: v0.12.1  
**ステータス**: ✅ Production Ready  
**テーマ**: Error Propagation & Timeout Features

---

## 🎉 概要

v0.12.1は、v0.12.0で導入されたasync/await機能の改善を含むパッチリリースです。**?オペレーター（エラー伝播）**と**タイムアウト機能**の追加により、**より使いやすく信頼性の高い非同期処理とエラーハンドリング**が実現されました。

**主な新機能**:
- ✅ ?オペレーター（Result<T, E>とOption<T>のエラー伝播）
- ✅ タイムアウト機能（基本実装）

**全テスト合格**: 750+テスト、42個のintegration tests、100%成功 ✅

---

## 🆕 新機能

### 1. ?オペレーター（エラー伝播）

**変更内容**:
Rustスタイルの`?`オペレーターで、`Result<T, E>`と`Option<T>`のエラー処理を簡潔に記述できるようになりました。

**使用例（Result<T, E>）**:
```cb
Result<int, string> chain_divide(int x) {
    int a = divide(x, 2)?;  // Errの場合は即座にreturn
    int b = divide(a, 3)?;
    return Result::Ok(b);
}
```

**使用例（Option<T>）**:
```cb
Option<int> find_and_double(int[] arr, int target) {
    int idx = find(arr, target)?;  // Noneの場合は即座にreturn
    return Option::Some(arr[idx] * 2);
}
```

**詳細**: [docs/features/question_operator_design.md](../docs/features/question_operator_design.md)

---

**タイムアウト機能の追加**:
async関数にタイムアウトを設定する基本機能を実装しました。指定時間内に完了しない場合、タイムアウトを検出してデフォルト値を返します。

**使用例**:
```cb
async int fast_task() {
    await sleep(100);  // 0.1秒かかる処理
    return 42;
}

async int slow_task() {
    await sleep(2000);  // 2秒かかる処理
    return 99;
}

async int main() {
    // タイムアウト機能の使用
    Future<int> f1 = timeout(fast_task(), 500);
    int result1 = await f1;  // 42 (完了)
    
    Future<int> f2 = timeout(slow_task(), 200);
    int result2 = await f2;  // 0 (タイムアウト時のデフォルト値)
    
    return 0;
}
```
```cb
async int fast_task() {
    await sleep(100);  // 0.1秒かかる処理
    return 42;
}

async int slow_task() {
    await sleep(2000);  // 2秒かかる処理
    return 99;
}

async int main() {
    // タイムアウト機能の使用
    Future<int> f1 = timeout(fast_task(), 500);
    int result1 = await f1;  // 42 (完了)
    
    Future<int> f2 = timeout(slow_task(), 200);
    int result2 = await f2;  // 0 (タイムアウト時のデフォルト値)
    
    return 0;
}
```

**実装内容**:
- ✅ `timeout()` builtin関数の追加
- ✅ `AsyncTask`にタイムアウトフィールド追加
- ✅ Event loopでのタイムアウトチェック処理
- ✅ タイムアウト検出とデフォルト値返却
- ✅ 複数タスクの同時タイムアウト管理

**制限事項**（将来バージョンで改善予定）:
- タイムアウト時は型のデフォルト値を返却（intなら0、structならデフォルト初期化値）
- Result<T, E>との完全な統合は未実装（v0.15.0予定）
- タイムアウト後のタスクキャンセル処理は未実装（タスクは実行継続）
- 複雑なジェネリック型でのタイムアウトは未検証

**テストカバレッジ**:
- ✅ 基本タイムアウト機能（fast/slow task）
- ✅ 複数タスクの同時タイムアウト
- ✅ ゼロタイムアウトのエッジケース
- ✅ Integration test suite（5テスト全て合格）

**詳細**: [docs/features/timeout_design.md](../docs/features/timeout_design.md)

---

## ✅ テスト結果

### 全テストスイート成功

```
✅ Integration tests: PASSED
   - Async tests: 32 tests
   - Question operator tests: 5 tests
   - Timeout tests: 5 tests
✅ Unit tests: PASSED
✅ Stdlib C++ tests: PASSED (33 tests)
✅ Stdlib Cb tests: PASSED

Test suites: 4/4 passed, 0/4 failed
Total tests: 750+
Success Rate: 100%
Test time: 12s
```

**新規テストカバレッジ**:
- `tests/cases/error_propagation/`: 8テスト（?オペレーター）
- `tests/cases/async/`: 3テスト（タイムアウト）
- `tests/integration/`: 2スクリプト（10インテグレーションテスト）

---

## 📚 関連ドキュメント

- [v0.12.0リリースノート](v0.12.0.md) - async/await基本機能
- [アーキテクチャドキュメント](../docs/architecture.md)
- [BNF仕様](../docs/BNF.md)
- [言語仕様](../docs/spec.md)

---

## 🎊 まとめ

v0.12.1は、v0.12.0で導入されたasync/await機能の改善とバグ修正を含むパッチリリースです。

### 主要達成事項

1. ✅ ?オペレーター（エラー伝播）実装
2. ✅ タイムアウト機能実装（基本実装）
3. ✅ 包括的なテストカバレッジ
4. ✅ 750+テスト全て成功
5. ✅ 互換性維持（破壊的変更なし）

### ステータス

```
Version: v0.12.1
Status: Production Ready ✅
Tests: 750+ (100% passing)
Breaking Changes: None
```

**Cb言語のエラーハンドリングと非同期処理がより強力になりました！** 🚀

---

**リリース日**: 2025年11月12日  
**バージョン**: v0.12.1  
**ステータス**: ✅ Production Ready
